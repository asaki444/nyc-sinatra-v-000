<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figures</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <label for = "figure[name]">New Figure</label>

      <input type = "text" id = "figure[name]" name = "figure_name">


      <label for = "title[name]">Title Name</label>
      <input type = "text" id="title[name]" name="new_title">

      <p><label for = "landmark[name]">Create New Landmark</label></p>

      <input type = "text" id = "landmark[name]" name = "new_landmark">
       Select Landmark: <br>
       <%Landmark.all.each do |landmark|%>
        <input type="checkbox"  id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%=landmark.id%>"><%=landmark.name%></input>
       <%end%>
       Select Title:<br>
       <% Title.all.each do |title| %>
 		     <li><input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>"><%= title.name %></input></li>
 	     <% end %>

      <input type = "submit" value = "Create New Figure">
    </form>

  </body>
</html>
